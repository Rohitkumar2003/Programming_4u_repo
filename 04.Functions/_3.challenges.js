
// ? 1.write a regular function that takes a string and returns it with the first letter capilized
    // function capilizedFirstLetter(str) {
    //     console.log(str.charAt().toUpperCase() + str.slice(1))
    // }
    // capilizedFirstLetter('alpha gamma beta')

// ? 2. Show an alert message that says "Please login" after 5 sec on your app.

    // setTimeout(()=>{
    //     alert("Please Login")
    // },5000)

// ? 3. Make an Arrow function that takes a price and discount, and returns the prize after discount 

// let discountPrize = (price, discount)=> {   
//     let discountPrize =  price-price*(discount/100);
//     return discountPrize;
// }

// let result = discountPrize(1043739, 20);
// console.log(result)

// ? 4. Create a function that buids a userName from a fullName
// function generatUserName(fullName) {
//     return "@" + fullName.trim().replaceAll(" ", "")    + fullName.length;
// }
// console.log(generatUserName('JohnDoe'))
// ? 5. Write a function that takes a traffic light color and gives the correct instruction (eg. "Go" for Green, "Stop" for Red, "Caution" for Yellow, and "invalid color " for anything else)

// let color = 'Green';
//     if(color === 'Green') {return "Go"}
//     else if(color === 'Red') {return `Stop`}
//     else if(color === 'Yellow') {return `Caution`}
//     else {return `Invalid Color `}


